<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Account Move Form - Delivery Notes + Discount Totals -->
        <record id="view_move_form_custom_fields" model="ir.ui.view">
            <field name="name">account.move.form.custom.fields</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>

            <field name="arch" type="xml">

                <!-- Delivery Notes after Shipping Address -->
                <xpath expr="//field[@name='partner_shipping_id']" position="after">
                    <field name="delivery_notes"
                           readonly="1"
                           invisible="move_type not in ('out_invoice', 'out_refund', 'out_receipt')"/>
                </xpath>

                <!-- Amount Before Discount + Discount -->
                <xpath expr="//field[@name='tax_totals']" position="before">
                    <group class="oe_subtotal_footer_separator" colspan="2">
                        <field name="amount_before_discount"
                               widget="monetary"
                               readonly="1"
                               string="Total Before Discount"/>

                        <field name="amount_discount"
                               widget="monetary"
                               readonly="1"
                               string="Discount Amount"/>
                    </group>
                </xpath>

            </field>
        </record>

    </data>
</odoo>
